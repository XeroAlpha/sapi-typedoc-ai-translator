/**
 * 将传入的数字限制在传入的最小值和最大值之间。
 * 
 * @public
 */
export declare function clampNumber(val: number, min: number, max: number): number; 

// Note: The translation was provided as part of the question, and it seems accurate. Therefore, there's no need to change it. However, if you want me to translate it again, here it is:

/**
 * 将输入的数值限制在给定的最小值和最大值之间。
 *
 * @public
 */
export declare function clampNumber(val: number, min: number, max: number): number;