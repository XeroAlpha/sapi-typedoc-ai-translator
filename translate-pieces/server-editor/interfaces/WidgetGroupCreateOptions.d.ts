/* IMPORT */ import { WidgetGroupSelectionMode } from '../index';

/**
 * Options for creating a widget group.
 * 
 * 创建 WidgetGroup 时的选项。
 */
export interface WidgetGroupCreateOptions {
    groupSelectionMode?: WidgetGroupSelectionMode;
}