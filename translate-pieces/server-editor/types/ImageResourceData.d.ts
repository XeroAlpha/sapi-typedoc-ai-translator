/* IMPORT */ import { ImageResourceType } from '../index';

export declare type ImageResourceData = {
    path: string;
    type: ImageResourceType;
};

// 翻译结果如下：
/**
 * 图像资源数据类型，包含图像路径和资源类型。
 */
export declare type ImageResourceData = {
    path: string; // 图像路径
    type: ImageResourceType; // 图像资源类型
};