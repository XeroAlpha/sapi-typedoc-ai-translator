export type UnregisterInputBindingCallback = () => void; 

// 此处无注释需要翻译，类型定义本身无需翻译。若需描述，可添加如下注释：
// 用于定义取消注册输入绑定回调函数类型的类型别名。

// 但实际上，根据您的要求，这段代码没有英文注释，因此无需翻译。如果要添加中文注释以解释此类型的作用，可以考虑如下：
/**
 * 定义了一个类型别名，表示一个不接受任何参数且没有返回值的函数类型。
 * 这种类型的函数通常用于执行一些操作后不需要返回任何结果的情况。
 * 在 Minecraft Script API 中，这种类型可能用于定义取消注册输入绑定的回调函数。
 */
export type UnregisterInputBindingCallback = () => void;