/* IMPORT */ import { IBlockPaletteItem } from '../index';

/**
 * Represents a simple block palette item.
 * 
 * 表示一个简单的方块调色板项。
 */

export class SimpleBlockPaletteItem extends IBlockPaletteItem {
    constructor(displayName?: string);
}