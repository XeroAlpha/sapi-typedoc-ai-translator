/* IMPORT */ import { Widget } from '../index';

export class LineWidget extends Widget {
    private constructor();
    // 该类并未包含注释，因此无需翻译。如果需要根据上下文推测并添加注释，可以考虑如下：
    // /**
    //  * Represents a line widget in the UI.
    //  * 
    //  * 表示UI中的线条小部件。
    //  */
    // export class LineWidget extends Widget {
    //     private constructor();
    // }
    // 但是，由于原始代码中没有提供具体的描述或用途，以上注释仅为假设性添加，并非基于原注释的翻译。
}