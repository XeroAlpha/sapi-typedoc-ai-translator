/**
 * Enum representing different states of the daylight cycle.
 * 
 * 表示日光循环不同状态的枚举。
 */
export enum DaylightCycle {
    Normal = 0, // 正常的日光循环
    AlwaysDay = 1, // 总是白天
    LockTime = 2, // 锁定时间
}