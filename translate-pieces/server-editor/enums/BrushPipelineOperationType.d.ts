export enum BrushPipelineOperationType {
    /**
     * Include elements within the brush.
     * 
     * 包含画刷内的元素。
     */
    Include = 0,
    /**
     * Exclude elements within the brush.
     * 
     * 排除画刷内的元素。
     */
    Exclude = 1,
}