export enum ProjectExportType {
    /**
     * Represents a playable world export type.
     * 
     * 表示可玩世界的导出类型。
     */
    PlayableWorld = 0,
    /**
     * Represents a project backup export type.
     * 
     * 表示项目备份的导出类型。
     */
    ProjectBackup = 1,
    /**
     * Represents a world template export type.
     * 
     * 表示世界模板的导出类型。
     */
    WorldTemplate = 2,
}