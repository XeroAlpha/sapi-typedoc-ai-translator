/* IMPORT */ import { Vector2 } from '../index';

/**
 * @beta
 */
export interface CameraFixedBoomOptions {
    viewOffset?: Vector2;
    /**
     * @beta
     * 
     * `viewOffset` 用于指定相机视图偏移量的 {@link Vector2}。
     * 
     * `viewOffset` 用于指定相机视图偏移量的 {@link Vector2}。
     */
}