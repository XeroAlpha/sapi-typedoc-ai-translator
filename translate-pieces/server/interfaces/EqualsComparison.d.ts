/**
 * @beta
 * Equal to operator.
 * 
 * 等于操作符。
 */
export interface EqualsComparison {
    /**
     * @remarks
     * Threshold value compared against.
     * 
     * 被比较的阈值。
     */
    equals: boolean | number | string;
}