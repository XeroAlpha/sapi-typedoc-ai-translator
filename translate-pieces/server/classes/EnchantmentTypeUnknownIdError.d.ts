/**
 * An error class for scenarios where an unknown enchantment ID is encountered.
 * 
 * 当遇到未知附魔ID时抛出的错误类。
 */
export class EnchantmentTypeUnknownIdError extends Error {
    private constructor();
}