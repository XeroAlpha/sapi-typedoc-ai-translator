/* IMPORT */ import { Player } from '../index';

/**
 * @beta
 */
export class PlayerEmoteAfterEvent {
    private constructor();
    readonly personaPieceId: string;
    readonly player: Player;
    /**
     * @beta
     * 
     * 表示玩家表情动作后事件。
     * 
     * 该类包含：
     * 
     * personaPieceId: string; —— 人物部件的ID。
     * 
     * player: Player; —— 执行表情动作的玩家对象。
     */
}